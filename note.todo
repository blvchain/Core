% BLVchain
    ! WebSocket
        x Debug reconnect to nodes that lost connection


    ! gRPC
        % Read data
            - add read data type of ws to grpc too
            - add get data from other nodes with ws if not exists

        % Add data
            - get prehashblock data from other nodes if not exists

        x Make auth protection

    ! General
        - Add config file for builded app and make path for it